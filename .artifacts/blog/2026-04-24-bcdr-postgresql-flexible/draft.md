# Draft Summary
PostgreSQL BCDR has clear-cut options: backups for corruption/accidental delete, read replicas for read scaling, geo-replication for region failure. Most teams only need backups + read replicas.
